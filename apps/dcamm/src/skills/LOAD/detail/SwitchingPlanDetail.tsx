import { DetailConfig } from '@dg3/schema';
import { SWITCHING_PLAN_WIDGET } from './SwitchingPlanJson';

export const SWITCHING_PLAN_DETAIL: DetailConfig = {
  id: 'switching_plan.detail',
  version: '1.0',
  widgets: [SWITCHING_PLAN_WIDGET],
};
