import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  placeholder: {
    defaultMessage: 'insert or write value',
    id: 'textInput.placeholder',
  },
  noOptions: {
    defaultMessage: 'no options',
    id: 'textInput.noOptions',
  },
});
