import { defineMessages } from 'react-intl';

import { Messages } from '@dg3/types';

export const messages: Messages = defineMessages({
  noDataFound: {
    defaultMessage: 'No data found',
    id: 'emptyData.noDataFound',
  },
});
