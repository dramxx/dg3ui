import { defineMessages } from 'react-intl';

import { Messages } from '@dg3/types';

export const messages: Messages = defineMessages({
  emptyReports: {
    defaultMessage: 'No reports available',
    id: 'reportsWindow.emptyReports',
  },
});
