import * as React from 'react';
import { storiesOf } from '@storybook/react';
import { UserSettings } from './UserSettings';

storiesOf('UserSettings', module).add('UserSettings', () => <UserSettings/>);
